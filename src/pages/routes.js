import React from 'react';
import { Route, IndexRoute } from 'react-router';

import Detail from './pages/routes';

const routes = (
    <Route path="/" component={ App }>
        {routes}
    </Route>
);

export default routes;
